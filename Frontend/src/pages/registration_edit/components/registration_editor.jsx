import React from 'react'
import styles from './editor.module.scss'

export default function RegistrationEditor({ user_id, competition_id }) {
  return <div className={styles.editor}>Registration Editing</div>
}
