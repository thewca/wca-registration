import './icons.scss'
import React from 'react'

//TODO move this to the WCA-component Library

export default function CubingIcon({ event }) {
  return <i className={`icon cubing-icon event-${event}`} />
}
